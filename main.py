from genericpath import isfile
import re, fileinput, os, pprint

from dir_paths import paths
from filter import filter_names


def main():
    paths()


# change this for file name regex name to delete

# if file_name_regex == file_name_regex + EXCLUSIONS[0]:
# change this to directory of path of deletable files

main()
